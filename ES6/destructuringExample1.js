const obj = {
  fName: "Ram",
  lName: "Kr."
}

const { Name, lName } = obj;

console.log("\nfirstName: " + obj.fName + "\nlastName: " + obj.lName);
console.log("\nFULL NAME: " + Name + "  " + lName); //var name must match with object properties name.